import fs from "node:fs";
export function makeOpenFileStreamFunction(filename) {
    return (mode) => {
        if (mode === "read") {
            return fs.createReadStream(filename);
        }
        return fs.createWriteStream(filename, { flags: "a" });
    };
}
