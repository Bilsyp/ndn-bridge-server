import { Level } from "level";
import { DataStore } from "./data-store_browser.js";
/** Create a persistent DataStore with `level`. */
export function makePersistentDataStore(location) {
    return DataStore.create(Level, location, {});
}
