/** Register a fixed set of prefixes. */
export function PrefixRegStatic(...prefixes) {
    return (store, face) => {
        for (const prefix of prefixes) {
            face.addRoute(prefix);
        }
        return { close: () => undefined };
    };
}
