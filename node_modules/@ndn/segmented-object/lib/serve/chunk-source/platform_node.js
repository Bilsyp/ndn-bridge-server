import fs from "node:fs/promises";
import { promises as zenfs } from "@zenfs/core";
export function fsOpen(path, opts) {
    if (opts.zenfs) {
        return zenfs.open(path, "r");
    }
    return fs.open(path, "r");
}
